/** Maximum number of toasts shown at once */
export const MAX_TOASTS = 3

/** Soroban transaction polling: max attempts before timeout */
export const TX_POLL_MAX_ATTEMPTS = 20

/** Soroban transaction polling: delay between attempts (ms) */
export const TX_POLL_INTERVAL_MS = 1500

/** Freighter wallet detection: max wait time (ms) */
export const FREIGHTER_DETECT_TIMEOUT_MS = 3000

/** Freighter wallet detection: poll interval (ms) */
export const FREIGHTER_DETECT_INTERVAL_MS = 100

/** Debounce delay for contract call buttons (ms) */
export const CONTRACT_CALL_DEBOUNCE_MS = 500

/** Stellar Testnet RPC URL */
export const STELLAR_TESTNET_RPC = 'https://soroban-testnet.stellar.org'

/** Stellar network passphrases */
export const STELLAR_TESTNET_PASSPHRASE = 'Test SDF Network ; September 2015'
export const STELLAR_MAINNET_PASSPHRASE = 'Public Global Stellar Network ; September 2015'
